Git Bash path:
cd Desktop
cd polymorphism-darkbloodprince

Git Push:
git add .
git status
git commit -m "First"
git push

Git Initial:
git init
git status
git add -A
git status
git commit -m "First"
git remote add origin https://github.com/acciojob/polymorphism-darkbloodprince.git
git push origin master

Git Clone:
cd Desktop
git clone "https://github.com/acciojob/polymorphism-darkbloodprince.git"


Another git process seems to be running in this repository, e.g.
an editor opened by 'git commit'. Please make sure all processes
are terminated then try again. If it still fails, a git process
may have crashed in this repository earlier:
remove the file manually to continue.

rm -f .git/index.lock
